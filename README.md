 npx hardhat node - Deploy Node

  npx hardhat run scripts/deploy.ts --network localhost and deploy_marketplace.ts or deploy_all

  Update .env with factory address

  uvicorn app.main:app --reload #for backend